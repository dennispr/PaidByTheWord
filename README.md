# TopWords

Add epub to workspace

Add your book to generate_wordset.js //const EPUB_FILES = ['PrideandPrejudice.epub','mobyDick.epub']; 

to generate word sets run
'node generate_wordset.js'

to edit metadata, look at books.pack.js

to see the project start a server by running
python3 -m http.server 8000